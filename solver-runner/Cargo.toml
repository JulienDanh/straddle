[package]
name = "solver-runner"
version = "0.1.0"
edition = "2021"

[dependencies]
postflop-solver = { path = "../postflop-solver" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
lto = "thin"
